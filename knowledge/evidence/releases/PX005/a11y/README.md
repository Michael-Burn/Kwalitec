# PX-005 accessibility sanity

Copy and identity changes reviewed for meaningful labels:

- Login badge uses `role="status"` with Private Beta title
- Help search and FAQ topics remain keyboard-reachable
- Diagnostic disclosure summary renamed; content still in `<details>`
- Session contention flash uses standard flash channel (visible, not assertive spam)
- Preparing skeleton uses `aria-busy` / visually-hidden label

Automated: student + session accessibility suites included in Phase 3 pack (**green**).
